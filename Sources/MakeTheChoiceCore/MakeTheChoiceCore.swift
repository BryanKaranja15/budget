import Foundation

/// Umbrella namespace + version marker for the MakeTheChoice core domain layer.
public enum MakeTheChoiceCore {
    /// Logical version of the core domain/data layer (independent of the app version).
    public static let version = "0.1.0"
}
